{
  "name": "eve",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "postcss global/globalStyles.css -o global/globalStyles.min.css --map",
    "lint:css": "stylelint 'global/**/*.css'",
    "format": "prettier --write '**/*.{html,css,md,json}'"
  },
  "devDependencies": {
    "postcss-cli": "^10.1.0",
    "cssnano": "^6.0.0",
    "stylelint": "^15.0.0",
    "prettier": "^2.0.0"
  }
}